#pragma once

// netaccess build configuration. Generated by CMake from config.h.in —
// do not edit the file in the build tree.
//
// Values come from project() (version) and add_compile_definitions()
// (feature toggles). Extend this header as the project grows.

#define NETACCESS_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define NETACCESS_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define NETACCESS_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define NETACCESS_VERSION_STRING "@PROJECT_VERSION@"