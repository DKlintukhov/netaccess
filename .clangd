# Read the compilation database generated by CMAKE_EXPORT_COMPILE_COMMANDS
# (build/Release/compile_commands.json) instead of relying on a copied file at
# the project root. cmake_layout() puts each build type in its own folder.
CompilationDatabase: build/Release

CompileFlags:
    Add:
        - -std=c++20
